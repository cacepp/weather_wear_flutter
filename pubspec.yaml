name: weather_wear_flutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.7.2

dependencies:
  carousel_slider: ^5.0.0
  intl: ^0.20.2
  flutter:
    sdk: flutter

  flutter_dotenv: ^5.2.1
  http: ^1.4.0
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  flutter_switch: ^0.3.2
  sqflite: ^2.4.2
  flutter_rating_bar: ^4.0.1
  url_launcher: ^6.3.1
  tflite_flutter: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_native_splash: 2.4.6

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - env/.env
    - assets/clothing_model.tflite
    - assets/clothing_model_ru.tflite